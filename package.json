{
  "name": "screenplay-claude",
  "version": "1.0.0",
  "description": "Professional screenplay editor — single-file React app served statically.",
  "private": true,
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --single"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
